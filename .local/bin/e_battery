#!/bin/sh

(
  upower -b \
    | awk \
      '/^\s*battery\s*$/{f=1; next} /^\s*History/{f=0} f {$1=$1; print}';
  echo "tlp profile: $(tlp-stat -m)";
) | sort | column -t -s ":"

read -n1 -s -r -p ""
